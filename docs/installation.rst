============
Installation
============

At the command line::

    $ pip install dj-tasks

Add ``dj_tasks`` to your ``INSTALLED_APPS1``:

.. code-block:: python

    INSTALLED_APPS = [
        ...
        "dj_tasks",
        ...
    ]
